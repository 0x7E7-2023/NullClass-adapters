## 新增 / 修改的适配器

- [ ] 学校名 / 教务系统类型：
- [ ] 取数方式（DOM 抓取 / 同源接口，接口请写路径）：
- [ ] `allowHosts` 里声明的域名，逐条说明为什么需要：
- [ ] fixtures 已脱敏（无姓名 / 学号 / 身份证号），且是**真实抓取**的输出
- [ ] 已在 `index.json` 里加索引条目，主仓库里 `./gradlew :importer:test` 通过
- [ ] 脚本全文可被审计：没有外发数据、没有埋点、没有读写凭证

<!-- 维护者会逐条核对上面的声明，并通读 extract.js / parse.js -->
