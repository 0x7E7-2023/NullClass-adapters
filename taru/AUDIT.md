# 安全审计 —— 塔里木大学教务适配器（`taru`）

- **审计对象**：`jw-adapters/taru/extract.js` + `jw-adapters/taru/parse.js`（+ `manifest.json`）。
- **上游出处**：`shiguang_warehouse` 的 `resources/TARU/taru_01.js`
  （`https://github.com/XingHeYuZhuan/shiguang_warehouse`，MIT）。
  快照 commit **`e62554a4034386b893bcd6813c7b2b64f8c730a3`**（2026-09-12 12:50:29 +0800），
  该文件的 git 作者与同目录 `adapters.yaml` 的 `maintainer` 都是 **星河欲转**。
- **上游 `adapters.yaml`**：`adapter_name: 塔里木大学正方教务`，
  `import_url: https://auth.taru.edu.cn/cas/login?service=https%3A%2F%2Fhall.taru.edu.cn%2FserviceHall`
  —— `loginUrl` 取自这里。
- **移植者**：0x7E7-2023　**审计日期**：2026-09-13
- **复审（2026-09-13，批次三对抗式验收后的修复轮）**：补了周次分隔符归一（`1～16周` / `1－16周` /
  `1−16周` / `1–16周` / `1—16周` / `1至16周` 只取到第 1 周）与「单双标记自成一段」（`1-16周,双` 等
  退化成每周都上）两处缺陷；另为「作息时间越界过滤」「作息不完整就整份不交」两条**原来没有用例看着**
  的分支各补一条用例。新增 `separators` / `times-invalid` / `times-incomplete` 三对 fixture，
  变异记录 M8-M12 见 §6；`basic` / `week-forms` 的期望值一个字节都没动，仍全 `MATCH`。
- **再修（2026-09-13，批次三空白口径统一轮）**：上一轮的 `weeksOf` 是「先全文删空白、再归一分隔符、
  再按逗号切段」—— 这条口径**有副作用**：空白在本域里也是分段符，全文删空白会把 `1-3周 5-9周`
  拼成 `1-35` → 按 1-30 截断成全周，**静默吞掉一段**。已按同批 `xawl` 定稿的统一口径改成两步：
  ① 破折号异体（含新认的「到」）**连同两侧空白**归一成 `-`；② 剩下的空白折成逗号、仍当分段符。
  `week-forms` 补「植物生理学」`1-3周 5-9周` 一行（期望 1-3 与 5-9 两个 block，手推），
  `reportedTotal` 相应 18 → 19；变异 M13 见 §6；其余四对 fixture 的期望值一个字节没动。
- **审计依据**：[移植手册 §5](https://github.com/0x7E7-2023/NullClass/blob/main/docs/jw-adapter-porting.md) 八条 + [批次三专项检查表 10 条](https://github.com/0x7E7-2023/NullClass/blob/main/docs/impl/2026-09-13-port-adapters-batch3.md)。
- **已有同族参照**：`jw-adapters/wenhua`（文华学院，同为正方新版 `jwglxt`，本件按它的结构写，
  但**编码逐条按 taru 自己的脚本与用例重写**，没有照抄它的 fixture 期望 —— 见 §5）。

---

## 1. 请求了哪些主机与路径（手册 §5 第 3 条、检查表第 9 条）

上游 `taru_01.js` 里硬编码了一个绝对地址：

```
POST https://jwgl.taru.edu.cn/jwglxt/kbcx/xskbcx_cxXsgrkb.html?gnmkdm=N2151
     body: xnm=<起始学年>&xqm=<3|12>
```

移植件沿用同一路径与同一 body，另外两处（读当前学年学期、读节次时间）是移植时加的：

| 谁 | 方法 | 地址 | 干什么 |
|---|---|---|---|
| extract.js | GET | `<教务主机>/jwglxt/kbcx/xskbcx_cxXskbcxIndex.html?gnmkdm=N2151&layout=default` | 读课表页上的学年学期下拉框（`#xnm` / `#xqm`）。**用户已经在课表页时这一步跳过**，直接读页面上的这两个字段 |
| extract.js | POST | `<教务主机>/jwglxt/kbcx/xskbcx_cxXsgrkb.html?gnmkdm=N2151` | 取课表（JSON，`kbList` 排课行）；body 与上游一致：`xnm=&xqm=` |
| extract.js | POST | `<教务主机>/jwglxt/kbcx/xskbcx_cxRjc.html?gnmkdm=N2151` | 取节次时间（JSON 数组）；body `xnm=&xqm=`。**取不到不算失败**，交给 parse.js 回落内置节次表并写进 warnings |

- `<教务主机>` = `jwBase()`：地址里带 `/jwglxt/` 时用**当前页面同源**，否则回落到常量
  `https://jwgl.taru.edu.cn`。**实际请求的主机只有一个**：`jwgl.taru.edu.cn`
  （本校正方 `jwglxt` 的部署主机）。本件两个脚本的绝对 URL 扫描
  （`grep -oE "https?://[A-Za-z0-9._:%-]+" jw-adapters/taru/*.js`）只有三处命中，
  没有一处是别的域：`extract.js` 里注释中的上游仓库地址、`extract.js` 里的课表主机常量、
  `parse.js` 里注释中的上游仓库地址。
- 请求头只有 `Content-Type` / `X-Requested-With` / `Accept` 三个固定值，**不带任何自定义令牌**；
  Cookie 由 WebView 按同源规则自己带上，脚本不读也不写它。

### `allowHosts` = `["jwgl.taru.edu.cn"]`，为什么是这一个

本校的登录是学校统一 CAS：上游 `loginUrl` 指向 `auth.taru.edu.cn`，而课表本体在
`jwgl.taru.edu.cn` —— **两者不同源**。所以「要不要把 CAS 登录域写进 `allowHosts`」这个问题，
答案是：**只写课表主机，不写 CAS 域**，依据是这两段代码：

1. **网络闸门只认 `loginUrl` 的主机 + `allowHosts`**
   （`feature/settings/src/main/kotlin/com/nullclass/feature/settings/jw/JwWebViewStep.kt:173-177`：
   `gate.allowedHosts += adapter.allowedHosts(hostOf(adapter.manifest.loginUrl))`，
   而 `JwAdapterPackage.allowedHosts` = `loginHost + manifest.allowHosts`）。
   提取期间 `JwNetworkGate.intercept` 会拦掉白名单之外的**一切**请求
   （同文件 `:119-126`，`frozen` 期间非白名单直接返回空响应；判定用 `:99-102` 的 `allows()`）。
   `loginHost` 是 `auth.taru.edu.cn`，**`scheduleUrlHint` 不参与闸门** ——
   所以不写 `jwgl.taru.edu.cn` 的话，取课表的 POST 会被自己的宿主拦掉，适配器 100% 失败。
2. **桥的 origin 白名单来自 `loginUrl` + `scheduleUrlHint` + `allowHosts`**
   （`importer/src/main/kotlin/com/nullclass/importer/jw/JwOriginRules.kt:15-27`）。
   写**精确主机名**才会在课表页上注入桥（`*.` 通配会被 `JwOriginRules` 跳过，见手册 §5）。
   本件不用 OCR / 提问，但精确主机名同样让 `__ncInput` / `__ncDone` 在课表页上可用。
3. **不写 `auth.taru.edu.cn`**：适配器**从不请求** CAS 域 —— 登录全程由用户在 WebView 里手工完成，
   脚本不碰登录表单、不提交、不读令牌。而它作为 `loginUrl` 的主机**已经被自动放行**，
   再写一条只是重复（清单越短越好）。
4. **不写通配 `*.taru.edu.cn`**：那会一并放行 `hall.taru.edu.cn`（服务大厅）等本适配器
   一个都不请求的主机；而且通配项拿不到桥。

`parse.js` **不发任何请求**（CI 用 Rhino 实跑，是纯函数；里面唯一的绝对 URL 是注释里的上游仓库地址），
所以 `allowHosts` 只与 `extract.js` 有关。

## 2. 读了什么

- **页面**：只读两个**下拉框的值/文本** —— 当前课表页的 `#xnm`（学年）与 `#xqm`（学期）；
  不在课表页时先取一次课表页 HTML，用 `DOMParser` 读**离线文档**里同样两个字段，不插进当前页面。
  另外读 `window.location` 拼请求地址。
  **不读** `localStorage` / `sessionStorage` / `document.cookie` / 登录表单 / 任何密码字段
  （静态扫描无命中，见 §1 的 grep）；**不写 DOM、不改表单、不点提交、不注入脚本**。
- **接口**：只有课表接口的返回体（课名、教师、教室、星期、节次、周次、教学班名、学分）与节次时间
  接口的返回体（第几节、起止时间）。正方课表响应里夹带的 `xsxx`（学号姓名等学生信息）
  **一律不带走、不进 fixture**：`extract.js` 只把 `kbList` 交出去。
- **输出**：`extract.js` 交出的就是一张原始数据清单 —— `term`（xnm / xqm / 两个文本）、
  `raw.kbList`（排课行原样）、`timeSlots`、`reportedTotal`、`today`（日期字符串，只在
  「学年读不出来」时用于推算第 1 周；脚本里读当前时间的话这条分支没法写 fixture）。
- **载荷里只有排课信息**：课名 / 教师 / 教室 / 星期 / 节次 / 周次 / 学期名 / 开学日 / 总周数 / 节次时间。

## 3. 手册 §5 八条逐条

| # | 检查项 | 结论 |
|---|---|---|
| 1 | 不碰凭据 | ✅ 没有 `password` / `pwd` / 登录表单读取，也没有 `localStorage` / `sessionStorage` / `document.cookie` 访问（grep 无命中）。唯一的「凭据」是浏览器自己带的会话 Cookie，脚本既不读内容也不把它发到别处。上游的 `shiguangBridge.showToast` / `showPrompt` / `showSingleSelection` / `notifyTaskCompletion` 一律没有移植 |
| 2 | 不外发 | ✅ 全脚本只有一处 `fetch`（`extract.js` 的 `request()` 包装），三个地址都由 `jwBase()` 拼成，主机只有 `jwgl.taru.edu.cn`；没有 `sendBeacon` / `WebSocket` / `EventSource` / `new Image().src` / 隐藏表单，没有任何第三方域（§1 的绝对 URL 扫描只命中注释里的上游仓库地址与本校课表主机常量） |
| 3 | 请求域可控 | ✅ 请求主机只有一个，`allowHosts` 写精确主机名、无通配，CAS 登录域不写（判断依据见 §1） |
| 4 | 只读课表 | ✅ 只请求 `/kbcx/xskbcx_cxXskbcxIndex.html`（课表页）、`/kbcx/xskbcx_cxXsgrkb.html`（学生课表）、`/kbcx/xskbcx_cxRjc.html`（节次时间），全在课表查询模块内。不碰成绩（`/cjcx/*`）、学籍、个人信息、缴费、校历。课表响应里夹带的学生信息不带走 |
| 5 | 不埋点 | ✅ 没有任何统计 / 上报 / 遥测，没有 `img` 打点，没有 `console` 之外的输出（两个脚本连 `console.log` 都没有） |
| 6 | 不 eval 远程代码 | ✅ 没有 `eval` / `new Function`；只对教务自己的响应做 `JSON.parse`（包在 `try` 里，失败返回 null） |
| 7 | 不写页面 | ✅ 不插入或修改 DOM、不改表单、不触发提交或点击。读页面只读两个下拉框的 `value` / `textContent`；取课表页 HTML 用的是 `fetch` + `DOMParser`（**离线文档**，不注入当前页面） |
| 8 | 不依赖用户输入之外的秘密 | ✅ 没有硬编码密钥、令牌或他人学号。脚本里的常量只有正方的菜单号 `N2151`、上下文路径 `/jwglxt`、课表主机 `jwgl.taru.edu.cn`，都是公开的页面参数 |

**结论：可以进内置库。** 全部请求落在本校教务主机上，只读课表，不碰凭据、不外发、不埋点、不写页面。

## 4. 批次三专项检查表 10 条逐条

| # | 检查项 | 落实 |
|---|---|---|
| 1 | **周次编码四写法** | ✅ `weeksOf()` **按逗号切段后、在每段自己的原文上**认「单」「双」（不是剥完括号再认 —— 那正是第一批 HBMU 塌成「每周都上」的原因）。四种写法各有 fixture 行：「1-16周(单)」「(单)1-16周」「1-16(单周)」「1-3,5-9周」混排，另加「双周1-16」「1-16(双周)」「1-16周（双）」全角括号。变异 M1 / M3 证明这些用例真的看着这条路径（§6）。**注意上游 `taru_01.js` 只认前两种**：它的 `(\d+)-(\d+)周` / `^(\d+)周` 要求数字紧挨着「周」，后两种会整段丢周次（且一声不吭），移植时按检查表补齐 |
| 1b | **周次分隔符与「标记自成一段」**（2026-09-13 补，对抗式验收实测的两条缺陷） | ✅ 两条上游同族也会踩的路径，各有一个专门的 `separators` 用例钉住：① **分隔符**只认半角减号时「1～16周」（U+FF5E）/「1－16周」（U+FF0D）/「1−16周」（U+2212）/「1–16周」（U+2013）/「1—16周」（U+2014）/「1至16周」六种写法都只取到第 1 周（15 周静默丢，且无告警），现在先经 `WEEK_DASH = /\s*[-～－−–—~至到]\s*/g`（破折号异体**连同两侧空白**归一到半角 `-`，`1到16周` 本轮起也认）再取区间，**同族 xawl / gsmc / qdhhc / cqcivc 对同一批输入都给出 1-16**；② 「单」「双」**自己成一段**时（「1-16周,双」「1-16周，双」全角逗号、「1-16周,双周」「双周,1-16周」「1-16周,单」）段里没有数字，老的 `continue` 会把它整段跳过 → 整门课退化成每周都上；现在绑到**最近的一个周次段**（前面有并进前一段，前面没有并进后面那一段），两头都没有周次段（整行只有「双」）则写出 warnings、不静默。变异 M8 / M9 / M12 各自证明这两条与新 warnings 真的有用例看着（§6） |
| 1c | **空白是分段符，不许全文删空白**（2026-09-13 空白口径统一轮） | ✅ 上一轮修复把 `weeksOf` 写成「先 `replace(/\s+/g,'')` 全文删空白再切段」，静默吞段：`1-3周 5-9周`（两段，无逗号）被拼成 `1-35` → 1-30 全周。本轮按同批 `xawl` 定稿的统一口径改两步：① 破折号连同两侧空白归一（`1 -16周(单)` / `1- 16周` 不再被劈开）；② 剩下的空白折成逗号、仍当分段符 —— `1-16周 双` 的标记自成一段（原行为不变）、`1-3周 5-9周` 切成两段。`week-forms` 补「植物生理学」一行钉住（期望 1-3 与 5-9 两个 ALL block，按规范手推）；变异 M13 证明这行真的看着这条路径（§6） |
| 2 | **括号里的纯数字不是周次** | ✅ `stripGroups()`：括号里只装「一个数字或数字区间」（`(1)`、`(1-2)`，半角全角都算）的整组连内容一起丢掉；其余括号只丢括号字符、内容留着。用例：「(1)1-8周」→ 1-8 周（不是第 1 周）、「3-4节(2)」→ 3-4 节（不是 2-4）、「(1-16周)」→ 1-16 周（括号包住整串时内容不能跟着删）。变异 M2 专打这一条 |
| 3 | **无星期表头兜底** | ✅ **不适用**：本校课表走接口，星期由 `xqj` 字段直接给出，没有「按表格列数猜星期」的余地；taru 的两个脚本里也没有 `row.length - 7` 这类按长度猜列的写法（`grep -n "row.length" jw-adapters/taru/*.js` 无命中）。接口没回 `kbList` 时直接报错（带「请重新登录」的提示），不猜。注：仓库里 `hblgxy` / `hniu` / `upc` / `xjie` 还留着 `row.length - 7` 的旧写法（批次三文档已列为本批之外单独处理），与本件无关 |
| 4 | **时间合法性** | ✅ `timeOf()` 只接受 `HH:mm` / `HH:mm:ss` 且 时 0-23、分 0-59，其余留空；`readTimes()` 再把「节号读不出 / 时间读不出 / 结束不晚于开始 / 同一节号重复」的条目剔掉并计数。写进 `periodTimes` 的只可能是能过载荷校验的值（`00:00–23:59`、结束晚于开始）；课表用到而教务没给的节次 → **整份不交**、改用内置默认表并在 warnings 里说明（真实作息与内置表混拼比哪一份单独用都糟，且用户看不出来）。这两支原来**一条用例都没有看着**（验收时把越界判断改成恒假、把 `complete` 改成恒真，两条老用例照旧全绿），2026-09-13 各补一条：`times-invalid`（教务节次时间里混进 `25:00` 与 `10:75` 两条坏数据 → 剔掉并计数；课表只用第 1-2 节，所以作息仍完整、照常交出）、`times-incomplete`（课表是 1-4 节连堂、教务只回第 1、2 节 → `periodTimes` 整份为空并在 warnings 里说明）。变异 M10 / M11 分别证明它们不是摆设（§6） |
| 5 | **warnings 上限** | ✅ **条数**：分支结构上最多 10 条（开学日 / 学期 / 总周数 / 节次时间 / 记录总数 / 跳过行 / 认不出的节次 / 超范围周次 / 认不出单双 / 只有单双标记没有周次范围 各一条），远在 20 条以内。**长度**：拼进 warnings 的教务原文样本（认不出的周次、节次写法）先经 `clip(…, 40)` 截断，收尾再对每条兜一道 200 字（`MAX_WARNING`）。实测：五条用例里最长的 warnings 是 76 字（`week-forms`、`separators`）；把教务原文换成 300 字的样本时，有兜底的最长仍是 76 字，**删掉兜底则涨到 338 字 —— 那会让整包被应用拒绝**（变异 M7 单独验过，见 §6） |
| 6 | **分页** | ✅ 正方 `xskbcx_cxXsgrkb` 不分页、一次返回整学期，没有「取完第一页就停」的问题；仍把响应里的记录总数（`total` / `totalCount` / `totalRows` …）带出来对账，`reportedTotal > 实际行数` 时写进 warnings（`week-forms` 用例：报 19 条只取到 16 条 → 出声） |
| 7 | **学期名** | ✅ 用教务自己的下拉框拼：「xnmText `2026-2027`」+「xqmText `第一学期`」→ `2026-2027学年第一学期`；`xqmText` 认不出「一/二/三」时退回正方代号（`3` / `12` / `16`）。适配器名（塔里木大学）不进学期名 |
| 8 | **teacher 拿不到就留空** | ✅ 教师为空写 `null`（上游是「缺 `xm` 就整行丢掉」，移植时改成允许为空）；教室同理。用例里「体育(三)」的 `teacher` 就是 `null`，「金工实习」的 `location` 是 `null`。变异 M5 证明这两行不是被丢掉的 |
| 9 | **`allowHosts`** | ✅ 精确主机名 `jwgl.taru.edu.cn`、无通配、不写 CAS 域，理由与代码依据见 §1；路径全部相对（`/jwglxt/...`），没有把主机名拼进请求体 |
| 10 | **变异测试** | ✅ 见 §6：12 处变异、每处都报出「改坏哪一处 → 哪条用例红了」（其中 M7 如实标为「fixture 盖不住」，另附定向验证） |

## 5. 与文华学院（`wenhua`）的对照片：同族能复用多少

两份上游脚本请求的是同一个接口路径、同一套正方 `kbList` 字段，所以**骨架**（`/jwglxt` 上下文路径、
`N2151` 菜单号、`xnm`/`xqm` 表单、`kcmc`/`xm`/`cdmc`/`xqj`/`jcs`/`zcd` 行字段）是共用的。
按测试方案 §3.2 的四层分开说：

| 层 | 对 taru 成立吗 | 依据 |
|---|---|---|
| 接口信封（路径、请求头、`{kbList:[…]}` 返回结构） | ✅ 成立 | 上游 taru 用的就是 `/jwglxt/kbcx/xskbcx_cxXsgrkb.html?gnmkdm=N2151`，与 wenhua 逐字相同 |
| 行字段名（`kcmc`/`xm`/`cdmc`/`xqj`/`jcs`/`zcd`） | ✅ 成立 | 上游 taru 逐个读这六个字段 |
| **周次编码** | ⚠️ **部分不同** | 上游 taru 的正则比 wenhua 那份窄：只认「数字紧挨着『周』」的两种写法，`1-16(单周)` / `1-3,5-9周` 会整段丢；移植件按检查表把四种写法补齐，**并拿 taru 自己的用例锁住** |
| **取数路径（要哪些参数、要不要先取学号）** | ⚠️ 不同 | 上游 taru 的 body 只有 `xnm=&xqm=`（本件照它），wenhua 那份多带 `kzlx=ck&xsdm=&kclbdm=&kclxdm=`；另有两处是移植时加的（读课表页 `#xnm`/`#xqm`、`xskbcx_cxRjc` 取节次时间） |
| **节次 / 作息** | ⚠️ 不同 | wenhua 有节次时间接口；上游 taru **没有**，它写死「夏季/非夏季」两张表再让用户选一张 |
| 分页行为 | ✅ 都没有分页 | 正方 `xskbcx` 一次返回整学期 |

**本件与上游的行为差异（都有意为之，逐条有理由）**：

1. **不问用户三件事**（上游 `showPrompt` 学年、`showSingleSelection` 学期 / 作息）：
   学年学期改成读课表页上的 `#xnm` / `#xqm`（用户自己切过的那个学期优先），要别的学期就在教务页面里切；
   作息改成向教务的节次时间接口要。
2. **作息时间不搬上游那两张写死的表**：哪一套（夏季 / 非夏季）现在生效只有教务知道，
   脚本里挑一张就是猜；取不到教务的节次时间时回落**应用内置默认表**并在 warnings 里说清楚
   （还点了「塔里木大学分夏季 / 非夏季两套作息」），用户去学期管理里改。
3. **教师、教室为空不再整行丢课**：上游 `if (!kcmc || !xm || !cdmc || !xqj || !jcs || !zcd) continue;`
   会把「没排教师」或「没排教室」的课**整门丢掉且不报警**。
4. **节次写法**：上游按 `-` 切、取首尾两段，遇到 `3-4节(2)` 末节是 `NaN`、整行静默丢；
   本件取数字的**最小值/最大值**，并认「两位一节」（`0102`、`0910`）。

## 6. 变异测试记录（检查表第 10 条、测试方案 §3.1）

方法：**在内存里**对 `parse.js` 源码做定点替换后重跑**全部五条用例**（全程不落盘，跑完用 sha256 核对文件未变，
`basic` / `week-forms` / `separators` / `times-invalid` / `times-incomplete` 全 `MATCH` 为绿）。
共 13 处变异 —— M1-M7 是移植时做的（当时只有 `basic` 与 `week-forms` 两条用例），
M8-M12 是 2026-09-13 补用例时做的（用来证明新补的用例真的在跑；它们对两条老用例一律绿，
这正是「老用例盖不住」的实证）。M13 是空白口径统一轮做的（方法不同：直接改文件 → 跑五条用例 →
改回，改回后 sha256 与改坏前逐字节一致 `977ca5cd936195622c56c09c913d1c416991cc378053bcb67b52528514b449a7`，
再跑一次五条用例仍全 `MATCH`）。

| # | 改坏哪一处 | basic | week-forms | 红在哪（实际输出 vs 期望） |
|---|---|---|---|---|
| — | 原始（未变异） | 绿 | 绿 | 五条用例全绿；`parse.js` sha256 `4ec23fcf…` |
| M1 | `weeksOf()` 里丢掉单双周标记（`onlyOdd` / `onlyEven` 恒为 false） | **红** | **红** | basic：「大学英语(三)」`1-15 ODD` → `1-16 ALL`，「大学物理(一)」`2-16 EVEN` → `2-16 ALL`；week-forms：六个单双周课程全塌成每周都上 |
| M2 | `SERIAL_PAREN` 永不匹配（括号里的纯数字序号不再整组丢掉） | 绿 | **红** | 「大学物理(一)」`(1)1-8周` → **8-11 周**（括号里的 1 被当成周次起点）；「马克思主义基本原理」`3-4节(2)` → **第 3 节**（`4` 与 `2` 在括号字符被删后粘成 `42`，超上限被剔） |
| M3 | 先剥括号（整组连内容一起删）再找单双 —— 第一批 HBMU 那个 bug | **红** | **红** | basic：「大学英语(三)」`1-16周(单)`、`大学物理(一)` `2-16周(双)` 的标记随括号消失 → 每周都上；week-forms：`(单)1-16周`、`1-16(单周)`、`1-16(双周)`、`1-16周（双）` 同样塌掉，且 `(1-16周)` 整行被当成「没周次」跳过（**13 门课变 12 门**） |
| M4 | 不认「两位一节」写法（`0102` / `0910`） | 绿 | **红** | 「大学计算机基础」`0102` 与「形势与政策」`0910` 两行的节次读不出来 → 整行被跳过（**13 门课变 11 门**） |
| M5 | 教师或教室为空就整行丢掉（上游原样） | **红** | **红** | basic 少两门课（**8 → 6**）：「体育(三)」教师为空、「金工实习」教室为空；week-forms 少「军训」（教师为空，**13 → 12**） |
| M6 | 跳过行不再进 warnings（静默丢课） | **红** | **红** | basic 的 warnings 6 → 5（少了「有 4 行课表数据缺…已跳过」），week-forms 8 → 7 |
| M7 | 删掉 warnings 的截断兜底（`clip(…, 40)` 与 200 字兜底） | 绿 | 绿 | **当时两条用例都盖不住这一条**（它们里面的教务原文样本都很短，截不截断结果一样）—— 所以另做了一次定向验证：把用例里的周次、节次原文换成 300 字，有兜底时最长 warnings 仍 76 字，删掉兜底则涨到 **338 字**，而载荷校验单条超 200 字会让**整包被拒**。这条变异如实记下来：它没被 fixture 看着，靠的是构造上的兜底与这次定向验证 |
| M8 | 单双标记段不再绑定到周次段（`if (!hasNumber[i]) { continue; } if (false) {`，即还原补 B1 之前的行为） | 绿 | 绿 | **`separators` 红**（12 处差异）：「1-16周,双」「1-16周，双」「1-16周,双周」「双周,1-16周」四行 `2-16 EVEN` → `1-16 ALL`，「1-16周,单」`1-15 ODD` → `1-16 ALL`，且「只有单双标记…」那条 warnings 消失（6 → 5 条）。**两条老用例照旧全绿** |
| M9 | 取消周次分隔符归一（`WEEK_DASH` 换成一个永不匹配的正则，还原 B11） | 绿 | 绿 | **`separators` 红**（6 处差异）：「1～16周」「1－16周」「1−16周」「1–16周」「1—16周」「1至16周」六行从 `1-16 ALL` 塌成 **`1-1 ALL`**（16 周丢 15 周），且没有任何告警。**两条老用例照旧全绿** —— 它们的 zcd 里一个非半角分隔符都没有 |
| M10 | `timeOf()` 的时 / 分越界判断恒假（`if (false) return '';`） | 绿 | 绿 | **`times-invalid` 红**（5 处差异）：`25:00` 与 `10:75` 两条坏数据被照收进 `periodTimes`（2 条 → 4 条，`25:00` 这种值会被载荷校验直接拒掉），「有 2 条读不出来」那条 warnings 消失（4 → 3 条）。**`times-incomplete` 与两条老用例照旧全绿** |
| M11 | `complete` 恒真（作息不完整也照交） | 绿 | 绿 | **`times-incomplete` 红**（3 处差异）：课表用到第 1-4 节、教务只给到第 2 节，`periodTimes` 仍交出第 1、2 节（空 → 2 条）—— 应用会以为作息已齐、不再补内置默认表。**`times-invalid` 与两条老用例照旧全绿** |
| M12 | 删掉「只有单双标记、没有周次范围」那条 warnings（`if (false) {`） | 绿 | 绿 | **`separators` 红**（2 处差异）：`zcd = 双` 那行只剩通用的「有 1 行课表数据缺…已跳过」，新用例的 warnings 由 6 条变 5 条。这条是 M8 之外的独立证明 —— 就算绑定逻辑对，**告警本身**也有用例看着 |
| M13 | `weeksOf` 第二步改回「全文删空白」（`replace(/\s+/g, '')`，还原空白口径统一轮之前的写法） | 绿 | **红**（5 处差异） | **`week-forms` 红**：「植物生理学」`1-3周 5-9周` 的两个 block（1-3 / 5-9 ALL）并成**一个 `1-30 ALL`**（`1-35` 按 1-30 截断，静默吞掉一段），且 `totalWeeks` 被抬高到 30、「超出 1-30 周」告警从 1 处变 6 处。**其余四条用例照旧全绿** —— 只有新加的这行在看着这条路径 |

M2 / M4 只让 `week-forms` 变红，M8-M12 只让补的 `separators` / `times-*` 变红，
**没有一条新变异动得了 `basic` / `week-forms`** —— 这正是**边界用例存在的理由**：
老用例盖不到括号序号、两位一节、周次分隔符、标记自成一段、作息越界与作息完整性这六条路径，
只有边界用例在看着它们（测试方案 §3.1 说的「钉住当前行为」而不是「钉住正确行为」，就是被这几条挡住的）。
M7 则相反：**它没被任何 fixture 看着**，说清楚比假装盖住了好。

M8-M12 的每一条都做过「改坏 → 跑 → 改回来」两遍：改回后重算 sha256 与改坏前**逐字节一致**
（`4ec23fcf034dbe20217097858ceae3774dc1a91366f9d4d4f4319a5ddf5ae5cf`，`sha256sum` 与 node `crypto` 两边一致），
再跑一次原始源码，五条用例仍全 `MATCH`。变异只在内存里做，`parse.js` 从未落盘改动。

## 7. 已知边界与没做的事

- **fixture 是合成的**（测试方案 §3 为移植件开的例外）：形状照上游脚本实际读取的字段编，
  每份 fixture 文件的 `_note` 都写明了这一点。它只保证「同样的输入永远得到同样的输出」，
  **不保证在真实教务页面上解析正确**。谁拿到真实 dump 请替换并重跑门。
- **没有真机验证**（维护者没有该校账号）：取数路径（`xskbcx_cxXsgrkb` 的 body、
  `xskbcx_cxRjc` 是否存在、课表页上的 `#xnm`/`#xqm`）来自上游脚本与同族正方件的经验，
  真机上可能与本件假设不同 —— 出了偏差会以「取不到课表」「学年学期读不出来」这类
  带提示的错误暴露，不会静默导错。
- **没有移植上游的三次交互**（学年输入、学期选择、作息选择），理由见 §5。
- **`extract.js` 不做解析**：周次、节次、合并、开学日推算全在 `parse.js`，
  `extract.js` 只把原始行交出去（CI 跑不到 `extract.js`，逻辑放那里等于没有回归）。
- **开学日是推算的**：正方课表接口不给开学日期，按「第一学期 = 9 月 1 日所在周的周一 /
  第二学期 = 2 月 20 日所在周的周一」推算，并且**每次都写进 warnings**（推算值和真值在库里
  长得一模一样，不说用户就没有机会发现）；学年也读不出来时退回「今天所在周的周一」并换一句说明。
- **没有碰**：`index.json`（由主 agent 统一加）、任何 Kotlin、任何既有适配器、任何文档。
